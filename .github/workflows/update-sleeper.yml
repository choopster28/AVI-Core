name: Update Sleeper Data

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  update-sleeper:
    runs-on: ubuntu-latest

    env:
      SLEEPER_LEAGUE_ID: ${{ secrets.SLEEPER_LEAGUE_ID }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run Sleeper Update
        run: |
          PYTHONPATH=src python -m avi.cli update-sleeper

      - name: Validate Sleeper Data
        run: |
          PYTHONPATH=src python -m avi.cli validate-sleeper

      - name: Commit changes
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"

          git add .

          if git diff --cached --quiet; then
            echo "No changes to commit."
          else
            git commit -m "Manual Sleeper update"
            git push
          fi
